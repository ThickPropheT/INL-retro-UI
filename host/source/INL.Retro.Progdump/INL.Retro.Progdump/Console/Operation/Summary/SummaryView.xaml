<UserControl x:Class="INL.Retro.Progdump.Console.Operation.Summary.SummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:INL.Retro.Progdump.Console.Operation.Summary"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance local:SummaryViewModel}">

    <StackPanel Grid.IsSharedSizeScope="True">

        <local:SummaryLine Label="Name:"        Value="{Binding Path=Rom.Name}"/>
        <local:SummaryLine Label="Developer:"   Value="{Binding Path=Rom.DeveloperName}"/>
        <local:SummaryLine Label="Region:"      Value="{Binding Path=Rom.RegionName}"/>
        <local:SummaryLine Label="Type:"        Value="{Binding Path=Rom.TypeName}"/>
        <local:SummaryLine Label="Version:"     Value="{Binding Path=Rom.Version}"/>
        <local:SummaryLine Label="Size:"        Value="{Binding Path=Rom.Size, StringFormat='{}{0} KB'}"/>
        <local:SummaryLine Label="Bank Size:"   Value="{Binding Path=Rom.BankSize, StringFormat='{}{0} KB'}"/>

    </StackPanel>
</UserControl>
